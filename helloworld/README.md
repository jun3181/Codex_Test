# HelloWorld Minecraft Plugin

`/hello`를 입력하면 `세계에 오신걸 환영해요!` 메시지를 출력하는 Paper 플러그인입니다.

## 사용 방법
1. `helloworld` 폴더로 이동
2. `python export.py` 실행
3. 빌드할 Paper 버전을 선택
4. 빌드 완료 후 생성된 JAR를 Paper 서버의 `plugins/` 폴더에 복사

## 명령어
- `/hello`: `세계에 오신걸 환영해요!` 출력
